#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wake-on-lan/bin/wake-on-lan" "$@"
  ret=$?
else 
  node  "$basedir/../wake-on-lan/bin/wake-on-lan" "$@"
  ret=$?
fi
exit $ret
